# /etc/nginx/site.conf -- nginx Gradle HTTP build cache
#
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    include /etc/nginx/cache.conf;

    location / {
        return 404;
    }
}
